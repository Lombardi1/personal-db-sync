SELECT
    t.typname AS enum_name
FROM
    pg_type t
JOIN
    pg_enum e ON t.oid = e.enumtypid
JOIN
    pg_namespace n ON t.typnamespace = n.oid
WHERE
    n.nspname = 'public' AND t.typname LIKE '%fornitore_tipo%';